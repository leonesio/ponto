<div class="card">
  <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
    <h5>Detalhes do Professor</h5>
    <a href="/professores" class="btn btn-light btn-sm">Voltar</a>
  </div>
  <div class="card-body">
    <div class="mb-4">
      <h6>Informações Pessoais</h6>
      <hr>
      <p><strong>Nome:</strong> {{professor.nome}}</p>
      <p><strong>Matrícula:</strong> {{professor.matricula}}</p>
      <p><strong>Email:</strong> {{professor.email}}</p>
      <p><strong>Status:</strong> 
        {{#if (eq professor.status "ativo")}}
          <span class="badge bg-success">Ativo</span>
        {{else}}
          <span class="badge bg-danger">Inativo</span>
        {{/if}}
      </p>
      <p><strong>Departamento:</strong> {{professor.Departamento.nome}}</p>
    </div>
    
    <div class="mt-4 d-flex justify-content-end">
      <a href="/professores/editar/{{professor.id}}" class="btn btn-warning me-2">
        <i class="fas fa-edit"></i> Editar
      </a>
      <form action="/professores/excluir/{{professor.id}}?_method=DELETE" method="POST" class="d-inline">
        <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir este professor?')">
          <i class="fas fa-trash"></i> Excluir
        </button>
      </form>
    </div>
  </div>
</div>